<p id="notice"><%= notice %></p>

<h1>Gazooys</h1>
<div class="gazooys-block">

<table>
  <thead>
    <tr>
      <th>Text</th>
      <th colspan="3"></th>
    </tr>
  </thead>
 
 <% @gazooys.reverse_each do |gazooy| %>
 <span><%= gazooy.text %>.</span>
 
 <% end %>
  <tbody>
    <% @gazooys.reverse_each do |gazooy| %>
        <tr class='gazooy'>
         <% @users.each do |item| %>
          <tr>
          <% if item.id == gazooy.id %>
              <% @profiles.each do |prof| %>
                  <% if prof.user_id == item.id %>
                     <%# <td><%= link_to image_tag(prof.avatar )"Moi", profile_path(prof.user_id), :class => 'avatar',:id => 'profile'  %>
                   <div class='gazooy-block'>
                    <% samtest = profile_path(prof.user_id)%>
                    <h1> <%profile_path(prof.user_id)%> </h1>
                    <a href="<% profile_path(prof.user_id) %>" class='avatar'>
                      <img src='<%= prof.avatar.thumb.url %>' class='avatar'>
                    </a>
 </div>
                     <%# <td><% image_tag(current_user.profile.avatar.url) %>
                      <td><%= prof.avatar.thumb.url %></td>
                 <% end %>
            <% end %>
         <% end %>
        <% end %>
          <td><%= gazooy.text %></td>
          <% if gazooy.user.present?%>
          <td><%= gazooy.user.email %></td>
          <%else %>
          <td>User isnt present</td>
          <% end %>
         
          <td><%= link_to 'Show', gazooy %></td>
          <td><%= link_to 'Edit', edit_gazooy_path(gazooy) %></td>
          <td><%= link_to 'Destroy', gazooy, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
         
    <% end %>

  </tbody>
</table>
</div>
<%= link_to 'Créer un gazooy', new_gazooy_path,  {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window', class: 'btn btn-primary btn-lg'}  %>
<div id="modal-window" class="modal hide fade" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content"></div>
  </div>
</div>
<br>
<%= link_to 'Gazooyer', new_gazooy_path %>
 <div class='col-md-4 jumbotron pages'>
        You're on Gazooyr !
        <% @pages.each do |item| %>
  <h2>Title: <%= item.title %><br /></h2>
<% end %>